# SmartAmbulance - Python Dependencies
# Generated for Python Flask project
# Install with: pip install -r requirements.txt

# ── Core Web Framework ──────────────────────────────────────────────────────
Flask==3.1.3
Werkzeug==3.1.6

# ── Database ─────────────────────────────────────────────────────────────────
mysql-connector-python==9.6.0

# ── Environment Variables ────────────────────────────────────────────────────
python-dotenv==1.2.2

# ── HTTP Requests ────────────────────────────────────────────────────────────
requests==2.32.5

# ── Flask Internals (auto-installed with Flask, pinned for reproducibility) ──
blinker==1.9.0
click==8.3.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3

# ── Requests Internals (auto-installed with requests, pinned for reproducibility) ──
certifi==2026.2.25
charset-normalizer==3.4.5
idna==3.11
urllib3==2.6.3

# ── Timezone Support ─────────────────────────────────────────────────────────
tzdata==2026.1
tzlocal==5.3.1

# ── Windows Console Colors (auto-installed on Windows) ───────────────────────
colorama==0.4.6
